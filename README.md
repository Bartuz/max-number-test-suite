# Tests for PPJ homework
*(feeling awesome doing `ruby` code for `java` lectures)*

RSpec `doc` output so far:

```
Array
  #maximum_if_implementation
    when unsupported number of elements is given
      raises an error
    when supported number of elements is given
      does not raise an error
      when all elements are different
        when the first number is maximum
          returns maximum number
        when the middle number is maximum
          returns maximum number
        when the last number is maximum
          returns maximum number
    when are elements are the same
      returns maximum number
      returns single number
    when two elements are the same
      returns maximum number
      returns single number
  #maximum_while_implementation
    returns meximum number (PENDING: Temporarily skipped with xit)
```
